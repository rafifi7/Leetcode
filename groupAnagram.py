class Solution(object):
    def groupAnagrams(self, strs):
        #create array of arrays
        answer = []
        #creates two hashmaps to be able to compare
        word1, word2 = {}
        #for loop that iterates through every word
        for i in strs:
            #for loop that iterates through every letter of every word
            for j in i:
                word1.set
        
        
        #problem with idea is that you cannot add the word to a bunch of hashmaps.